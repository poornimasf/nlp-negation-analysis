version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - cd negation-analyzer
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: negation-analyzer/build
        files:
          - '**/*'
      cache:
        paths:
          - negation-analyzer/node_modules/**/*
